const greet = (message, name) => {
    console.log(`${message}, ${name}!`)
  }
  
  greet('Hello', 'John');
  greet('Hola', 'Antonio');
  greet('Ciao', 'Luigi')

  /* Answer: This code is DRY because it uses a function to about repeating code. */ 